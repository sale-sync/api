{
  "name": "sale-sync-media-s3-event",
  "version": "1.0.0",
  "description": "S3 Event Handler - Updates media status after upload",
  "main": "s3-event.ts",
  "type": "module",
  "scripts": {
    "bundle": "esbuild s3-event.ts --bundle --platform=node --target=node22 --outdir=bundle --external:@aws-sdk/*"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.895.0",
    "@aws-sdk/lib-dynamodb": "^3.895.0",
    "@aws-sdk/client-s3": "^3.895.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "@types/node": "^22.0.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.7.0"
  }
}