{
  "name": "@sale-sync-api/auth",
  "version": "1.0.0",
  "description": "Sale Sync api for auth",
  "main": "app.js",
  "author": "SAM CLI",
  "license": "MIT",
  "scripts": {
    "unit": "jest",
    "lint": "eslint '*.ts' --quiet --fix",
    "compile": "tsc",
    "bundle": "esbuild app.ts --bundle --platform=node --format=cjs --target=es2020 --sourcemap --outfile=bundle/app.js",
    "bundle:watch": "esbuild app.ts --bundle --platform=node --format=cjs --target=es2020 --sourcemap --outfile=bundle/app.js --watch",
    "test": "npm run compile && npm run unit"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.893.0",
    "@aws-sdk/lib-dynamodb": "^3.893.0",
    "@devyethiha/samjs": "^0.1.18",
    "@sale-sync/shared": "file:../packages",
    "esbuild": "^0.14.14",
    "install": "^0.13.0",
    "npm": "^11.6.2",
    "openid-client": "^5.7.1"
  },
  "devDependencies": {
    "@jest/globals": "^29.2.0",
    "@types/aws-lambda": "^8.10.92",
    "@types/jest": "^29.2.0",
    "@types/node": "^20.5.7",
    "@typescript-eslint/eslint-plugin": "^5.10.2",
    "@typescript-eslint/parser": "^5.10.2",
    "eslint": "^8.8.0",
    "eslint-config-prettier": "^8.3.0",
    "eslint-plugin-prettier": "^4.0.0",
    "jest": "^29.2.1",
    "prettier": "^2.5.1",
    "ts-jest": "^29.0.5",
    "ts-node": "^10.9.1",
    "typescript": "^4.8.4"
  }
}
