{
  "name": "queries-privacy-policy",
  "root": "apps/queries-api/privacy-policy",
  "sourceRoot": "apps/queries-api/privacy-policy",
  "projectType": "application",
  "targets": {
    "compile": {
      "executor": "nx:run-commands",
      "options": { "command": "npm run compile", "cwd": "apps/queries-api/privacy-policy" }
    },
    "bundle": {
      "executor": "nx:run-commands",
      "options": { "command": "npm run bundle", "cwd": "apps/queries-api/privacy-policy" }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": { "command": "npm test", "cwd": "apps/queries-api/privacy-policy" }
    },
    "unit": {
      "executor": "nx:run-commands",
      "options": { "command": "npm run unit", "cwd": "apps/queries-api/privacy-policy" }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": { "command": "npm run lint", "cwd": "apps/queries-api/privacy-policy" }
    }
  }
}
