{
  "name": "queries-testimonials",
  "root": "apps/queries-api/testimonials",
  "sourceRoot": "apps/queries-api/testimonials",
  "projectType": "application",
  "targets": {
    "compile": {
      "executor": "nx:run-commands",
      "options": { "command": "npm run compile", "cwd": "apps/queries-api/testimonials" }
    },
    "bundle": {
      "executor": "nx:run-commands",
      "options": { "command": "npm run bundle", "cwd": "apps/queries-api/testimonials" }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": { "command": "npm test", "cwd": "apps/queries-api/testimonials" }
    },
    "unit": {
      "executor": "nx:run-commands",
      "options": { "command": "npm run unit", "cwd": "apps/queries-api/testimonials" }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": { "command": "npm run lint", "cwd": "apps/queries-api/testimonials" }
    }
  }
}
